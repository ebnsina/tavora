DATABASE_URL=postgres://localhost:5432/tavora?sslmode=disable
ADDR=:8080
CORS_ORIGIN=http://localhost:5173
UPLOAD_DIR=./uploads
# How sign-in codes are texted to customers. "log" prints them to this server log (local only).
SMS_PROVIDER=log
